{
  "name": "product-fit-studio",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "build": "npm test && node build.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
